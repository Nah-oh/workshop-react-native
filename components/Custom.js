import React from 'react';
import { Text, View } from 'react-native';

export default class Custom extends React.Component {
    render () {
      return (
        <Text>C'est donc ça le futur de l'innovation</Text>
    )}
  }
  